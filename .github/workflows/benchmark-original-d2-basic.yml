name: Original D2 pi benchmark

on:
  pull_request:
    branches: [ main ]

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y build-essential libgmp-dev
      - name: Build benchmark
        run: |
          mkdir -p build
          gcc -O3 -march=native -std=c17 -Wall -Wextra -Wpedantic \
            -Isrc validation/benchmark_original_d2_basic_vs_chud.c \
            src/ramanujan_c_common.c -lgmp -lm \
            -o build/benchmark_original_d2_basic_vs_chud
      - name: Run benchmark
        run: |
          build/benchmark_original_d2_basic_vs_chud 1000 10000 30000 100000 | tee build/original_d2_vs_chud.csv
      - name: Upload benchmark table
        uses: actions/upload-artifact@v4
        with:
          name: original-d2-vs-chud
          path: build/original_d2_vs_chud.csv
